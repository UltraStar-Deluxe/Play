public enum ENoteType
{
    // 1 Pointweight, pitch
    Normal,
    // 2 Pointweight, pitch
    Golden,
    // 0 Pointweight, no pitch
    Freestyle,
    // 1 Pointweight, no pitch
    Rap,
    // 2 Pointweight, no pitch
    RapGolden
}
